# 뉴스레터 생성모듈 업데이트 — Gap Analysis (Check)

> **Feature**: 뉴스레터-생성모듈-업데이트
> **Design**: docs/02-design/features/뉴스레터-생성모듈-업데이트.design.md
> **분석일**: 2026-07-05
> **분석자**: gap-detector agent
> **Match Rate**: **98.2%** (28항목: ✅27 + ⚠️1) — 90% 임계값 통과 ✅

---

## 1. 항목별 판정 요약

| Design 섹션 | 항목 수 | ✅ | ⚠️ | ❌ |
|-------------|:------:|:--:|:--:|:--:|
| §2 DB Design (테이블·RLS·인덱스·bf 스키마) | 4 | 4 | — | — |
| §3 State Design (상태·재사용 원칙) | 3 | 3 | — | — |
| §4 F-1 저장·불러오기 (syncSections~offerPublish) | 8 | 8 | — | — |
| §5 F-2 커스터마이징 (치환·폰트·UI·bold·그리드) | 5 | 4 | 1 | — |
| §6 F-3 편집 UX (편집·라벨·다운로드·토글) | 6 | 6 | — | — |
| §8 Risk 대응 (프로젝트 전환·이미지 병합) | 2 | 2 | — | — |
| **합계** | **28** | **27** | **1** | **0** |

**⚠️ 유일한 부분 일치**: §5.2 폰트 적용 — 설계 초안은 "themed=spread override / email·grid=출력 치환" 2계층이었으나, 구현은 **전 템플릿 출력 치환으로 통일**. 결과 동일(의도 충족). → 설계 문서를 구현 방식으로 갱신 완료 (문서-구현 동기화).

Babel 훅 규칙 위반: **없음** (신규 훅은 전부 ProjectManagementSystem 최상위).

## 2. 발견된 결함 및 조치

| # | 심각도 | 내용 | 조치 |
|---|--------|------|------|
| 1 | 보안·낮음 | themed 4종 `sectionLabel(label)`이 사용자 편집 라벨을 **미이스케이프** 삽입 (email/grid는 이스케이프됨 — 정책 불일치, self-XSS 가능) | ✅ **수정 완료** — `_nlRenderSections` 3개 호출부에 `esc()` 적용, 구문 재검증 통과 |
| 2 | 정보 | `applyCustomDesign(html)` 시그니처가 설계 `(html, template, custom)`와 다름 (클로저 참조) — 기능 동일 | ✅ 설계 문서 갱신 |
| 3 | 정보·수용 | grid 템플릿은 카드 통합 레이아웃이라 boards/schedules 라벨 변경이 출력에 미반영 (gallery 뱃지만 반영) — 구조 특성 | 문서화 (수용) |
| 4 | 정보·수용 | bold 헤더 그라디언트의 짝색(#4c1d95)은 accent 치환 대상 아님 — 커스텀 색과 조합 불일치 가능 | 설계 §5.4에서 기 수용 |

## 3. 런타임 검증 결과 (Do 단계 verify 병행)

Playwright E2E로 확인 완료:
- 위저드 전체 주행 (Step 1→2→3), 흰 화면 없음
- bold + 커스텀 빨강 → 미리보기·다운로드 HTML에 완전 치환 (#7c3aed 0회)
- 섹션 라벨·인라인 제목 편집 → 미리보기 즉시 반영
- Step 2 왕복 후 편집·라벨 유지 (syncSections), 기본값 복원 시 원색 복귀 (회귀 없음)
- HTML 다운로드 4.2KB 생성, 임시저장은 테이블 미생성 상태에서 안전한 에러 처리

## 4. 미검증 항목 (환경 제약)

- **저장/불러오기/복제/발행의 서버 왕복**: `supabase-migration-newsletters.sql` 미적용 상태 (Supabase SQL Editor 실행 필요). 코드 경로는 설계와 일치 확인, 에러 처리 검증 완료.

## 5. 결론

Match Rate 98.2% ≥ 90% → **iterate 불필요**, Report 진행 가능.
결함 1건은 Check 단계 내 즉시 수정 완료. 남은 사용자 액션: 마이그레이션 적용 후 저장 기능 확인.
